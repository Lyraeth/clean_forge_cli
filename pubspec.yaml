name: clean_forge_cli
description: A powerful CLI for generating Flutter Clean Architecture files and stubs.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.5

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  forge: bin/cleam_forge_cli